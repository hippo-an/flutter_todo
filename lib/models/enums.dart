enum Urgent {
  urgent,
  notUrgent,

}

enum Importance {
  importance,
  notImportance
}

enum Priority {
  strongest,
  strong,
  normal,
  weak,
  weakest,
}

enum Progression {
  ready,
  progress,
  done,
  abort,
}